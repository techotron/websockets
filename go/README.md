# Go Websocket Server
A simple websocket server written in Go

Use `websocat` for the WS client to test out the server

```
websocket ws://localhost:8080/
```

Sending "start" will trigger the notification loop from the server
